---
all:
  hosts:
    jetson-orin:
      ansible_host: 192.168.1.XXX        # ← set to your Jetson's IP
      ansible_user: jetson               # ← set to the username you created during OOBE
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519
      ansible_python_interpreter: /usr/bin/python3
